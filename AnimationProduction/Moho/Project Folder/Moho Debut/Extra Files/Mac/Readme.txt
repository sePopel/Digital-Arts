For Mac users:

If you would like to Run Moho in low resolution mode, launch Moho using the command in this folder.

You can also do this from the command line or with autmated scripts by using one of the follwoing commands:

open -a Moho --args -AppleMagnifiedMode YES

open /Applications/Moho.app --args -AppleMagnifiedMode YES
